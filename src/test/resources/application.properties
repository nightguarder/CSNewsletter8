# Test overrides: disable Camunda/Zeebe clients so unit/integration tests run without a running engine
# Disable the Camunda client auto-activation used in production
camunda.client.enabled=false

# Run the web app on a random port during tests to avoid clashes
server.port=0
